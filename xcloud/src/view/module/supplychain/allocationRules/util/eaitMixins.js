export default {
  computed: {
    isShow() {
      return this.tabkey == this.comKey ? true : false;
    },
  },
  // methods: {
  //   isShow() {
  //     return this.tabkey == this.comKey ? true : false;
  //   },
  // },
};
